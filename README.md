# RagicLite

一個基於中繼資料驅動（Metadata-Driven）的輕量級無代碼表單與資料庫管理系統（DBMS），概念參考 Ragic。後端採用 ASP.NET Core 建置，支援動態定義表單欄位，並將資料以結構化 JSON 儲存與驗證。

---

## 核心特性 (MVP)

- **動態結構定義 (Metadata Schema)**：無需在資料庫手動執行 DDL（`CREATE TABLE`），隨時透過 API 定義新表單與擴充欄位。
- **動態欄位驗證 (Validation Engine)**：依據表單欄位型態與規則（必填、格式、選項），在寫入資料時動態校驗資料正確性。
- **靈活 JSON 儲存**：結合關聯表與 JSON 資料欄位，兼顧關聯完整性與欄位擴充彈性。
- **支援欄位型態**：
  - `Text`：單行文字
  - `Number`：數值型別（整數/浮點數）
  - `Date`：日期格式
  - `SingleSelect`：單選下拉式清單

---

## 採用 Clean Architecture 概念簡化分層：

RagicLite/
├── RagicLite.Domain/          # 核心領域實體 (Sheet, Field, Record, FieldType)
├── RagicLite.Infrastructure/  # EF Core 資料庫上下文與遷移 (AppDbContext)
├── RagicLite.Application/     # 業務邏輯、DTO 與動態驗證引擎 (DynamicValidator)
└── RagicLite.WebApi/          # RESTful API 端點與 Swagger 文件

## 系統架構設計 (System Architecture)

```text
+-----------------------------------------------------------------------+
|                           前端呈現層 (Presentation Layer)              |
|        UI 渲染器 (動態表單 / 試算表 Grid / 欄位拖拉設計器 / 資料過濾面板)       |
+-----------------------------------┬-----------------------------------+
                                    │ HTTP / RESTful API / JSON
+-----------------------------------▼-----------------------------------+
|                        API 閘道與通訊層 (API Gateway & Auth)          |
|                 身分驗證 (JWT) / 租戶識別 (Tenant Context)              |
+-----------------------------------┬-----------------------------------+
                                    │
+-----------------------------------▼-----------------------------------+
|                     核心應用服務層 (Core Application Services)         |
|  ┌───────────────────────┐  ┌──────────────────┐  ┌────────────────┐  |
|  │  中繼資料管理模組      │  │  資料記錄 CRUD 模組 │  │  匯入匯出模組  │  |
|  │  (Schema / Form Admin)│  │  (Record Service)│  │  (CSV / Excel) │  |
|  └───────────────────────┘  └──────────────────┘  └────────────────┘  |
+-----------------------------------┬-----------------------------------+
                                    │
+-----------------------------------▼-----------------------------------+
|                 動態運算與規則引擎 (Dynamic Execution Engine)         |
|  ┌───────────────────┐  ┌────────────────────┐  ┌──────────────────┐  |
|  │ 動態型別校驗器     │  │ 公式計算求值引擎   │  │ 關聯參照處理器   │  |
|  │ (DynamicValidator)│  │ (Formula Evaluator)│  │ (Link & Load)    │  |
|  └───────────────────┘  └────────────────────┘  └──────────────────┘  |
+-----------------------------------┬-----------------------------------+
                                    │
+-----------------------------------▼-----------------------------------+
|                     資料存取與抽象層 (Data Access & Query Engine)      |
|  ┌─────────────────────────────────┐  ┌────────────────────────────┐  |
|  │   查詢轉譯器 (Query Translator)  │  │   EF Core DbContext        │  |
|  │   (動態篩選/排序 -> JSON 查詢語法)│  │   (結構化資料存取)          │  |
|  └─────────────────────────────────┘  └────────────────────────────┘  |
+-----------------------------------┬-----------------------------------+
                                    │ SQL / JSONB Query
+-----------------------------------▼-----------------------------------+
|                        實體儲存層 (Physical Storage Layer)             |
|          PostgreSQL (jsonb + GIN 索引) 或 SQL Server (OPENJSON)       |
+-----------------------------------------------------------------------+
